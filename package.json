{
  "name": "json2officexml",
  "description": "Minimalist JSON to XLS (Excel Office XML) writer.",
  "dependencies": {
    "xmlbuilder" : ">= 0.3.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">= 0.1.100"
  }
}
